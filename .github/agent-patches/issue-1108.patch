diff --git a/CMakeLists.txt b/CMakeLists.txt
index 8eb7a63..0364ce6 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -247,6 +247,15 @@ add_test(NAME trace_issue_1114_latias_burnet_preempts_gladion
 add_test(NAME trace_issue_1110_tate_yields_to_burnet
   COMMAND regidrago_sim --simulate-this --scenario strict-jit/go-second --seed 182 --require-ready-by 3)
 
+# Known K1 VSTAR plus Professor Burnet must outrank irrelevant Gladion recovery:
+# https://api.pokemontcg.io/v2/cards/sm7-145
+# https://api.pokemontcg.io/v2/cards/swsh12tg-TG26
+# https://api.pokemontcg.io/v2/cards/swsh12-136
+# https://www.pokemon.com/us/pokemon-tcg/rules
+# https://github.com/FlareZ123/pokemon-sims/issues/1108
+add_test(NAME trace_issue_1108_known_burnet_steven_route
+  COMMAND regidrago_sim --simulate-this --scenario strict-jit/go-second --seed 101 --require-ready-by 3)
+
 add_test(NAME regidrago_sim_matrix_smoke COMMAND regidrago_sim --trials 100 --seed 20260705 --out ${CMAKE_CURRENT_BINARY_DIR}/smoke_results.csv)
 set_tests_properties(regidrago_sim_matrix_smoke PROPERTIES RUN_SERIAL TRUE)
 
diff --git a/src/trace_engine_v2/part_issue_1030_gladion_turo_override.inc b/src/trace_engine_v2/part_issue_1030_gladion_turo_override.inc
index 5a29c8c..e068f49 100644
--- a/src/trace_engine_v2/part_issue_1030_gladion_turo_override.inc
+++ b/src/trace_engine_v2/part_issue_1030_gladion_turo_override.inc
@@ -117,6 +117,38 @@
   }
 
   bool play_gladion() {
+    const bool held_burnet_vstar_completion = supporter_allowed() &&
+        state_.turn == 3 && scenario_.dci == DciProfile::StrictJit &&
+        state_.active && state_.active->card == Card::RegidragoV &&
+        state_.active->entered_turn < state_.turn &&
+        state_.active->grass >= 2 && state_.active->fire >= 1 &&
+        hand_count(Card::RegidragoVstar) > 0 &&
+        hand_count(Card::ProfessorBurnet) > 0 && !has_vstar() &&
+        need_payload() && can_play_payload_this_turn() &&
+        payload_might_be_in_deck();
+    if (held_burnet_vstar_completion) {
+      // The banked VSTAR may evolve the prior-turn Basic and Burnet supplies the
+      // strict-JIT payload during this turn. Gladion and a discard Item spend extra
+      // discrete resources without reaching readiness earlier:
+      // Gladion: https://api.pokemontcg.io/v2/cards/sm4-95
+      // Professor Burnet: https://api.pokemontcg.io/v2/cards/swsh12tg-TG26
+      // Regidrago VSTAR: https://api.pokemontcg.io/v2/cards/swsh12-136
+      // Supporter and evolution procedure: https://www.pokemon.com/us/pokemon-tcg/rules
+      // Resource-preserving earliest route: https://github.com/FlareZ123/pokemon-sims/blob/main/docs/POLICY_DECISIONS.md#decision-priorities
+      // Confirmed bug: https://github.com/FlareZ123/pokemon-sims/issues/1108
+      return false;
+    }
+    if (known_burnet_t3_steven_route_available()) {
+      // Steven reserves the known VSTAR and Professor Burnet for the deterministic
+      // next-turn finish. A backup Basic Prize exchange advances no missing axis:
+      // Gladion: https://api.pokemontcg.io/v2/cards/sm4-95
+      // Steven's Resolve: https://api.pokemontcg.io/v2/cards/sm7-145
+      // Professor Burnet: https://api.pokemontcg.io/v2/cards/swsh12tg-TG26
+      // Regidrago VSTAR: https://api.pokemontcg.io/v2/cards/swsh12-136
+      // Earliest-ready policy: https://github.com/FlareZ123/pokemon-sims/blob/main/docs/POLICY_DECISIONS.md#decision-priorities
+      // Confirmed bug: https://github.com/FlareZ123/pokemon-sims/issues/1108
+      return false;
+    }
     if (held_payload_completes_current_turn_without_gladion()) {
       // The held Dragon and legal one-discard Item already reach the same earliest
       // ready state. Preserve Gladion, the Supporter play, and Prize configuration:
diff --git a/src/trace_engine_v2/part_issue_1030_steven_turo_override.inc b/src/trace_engine_v2/part_issue_1030_steven_turo_override.inc
index 608e027..082e3a5 100644
--- a/src/trace_engine_v2/part_issue_1030_steven_turo_override.inc
+++ b/src/trace_engine_v2/part_issue_1030_steven_turo_override.inc
@@ -1,3 +1,61 @@
+  bool known_burnet_t3_steven_route_available() const {
+    if (!deck_seen_ || state_.turn != 2 || scenario_.going_first ||
+        scenario_.dci != DciProfile::StrictJit ||
+        scenario_.locks != LockMode::None || scenario_.max_turn < 3 ||
+        !supporter_allowed() || !state_.active ||
+        state_.active->card != Card::RegidragoV ||
+        state_.active->entered_turn >= state_.turn ||
+        state_.active->grass < 2 || state_.active->fire < 1 ||
+        hand_count(Card::StevensResolve) == 0 ||
+        hand_count(Card::RegidragoVstar) > 0 ||
+        hand_count(Card::ProfessorBurnet) > 0 ||
+        hand_count(Card::BrilliantBlender) > 0 ||
+        !need_vstar() || !need_payload()) {
+      return false;
+    }
+
+    // At K1, Steven can reserve the known VSTAR and Professor Burnet, then end T2.
+    // On T3 the prior-turn Active evolves and Burnet searches and discards the
+    // permitted Dragon payload during the strict-JIT ready turn:
+    // Steven's Resolve: https://api.pokemontcg.io/v2/cards/sm7-145
+    // Professor Burnet: https://api.pokemontcg.io/v2/cards/swsh12tg-TG26
+    // Regidrago VSTAR: https://api.pokemontcg.io/v2/cards/swsh12-136
+    // Core evolution, Supporter, search, discard, and turn procedure: https://www.pokemon.com/us/pokemon-tcg/rules
+    // K1 and earliest-ready policy: https://github.com/FlareZ123/pokemon-sims/blob/main/docs/POLICY_DECISIONS.md#knowledge-states
+    // https://github.com/FlareZ123/pokemon-sims/blob/main/docs/POLICY_DECISIONS.md#decision-priorities
+    // Confirmed bug: https://github.com/FlareZ123/pokemon-sims/issues/1108
+    return deck_count_after_search_started(Card::RegidragoVstar) > 0 &&
+        deck_count_after_search_started(Card::ProfessorBurnet) > 0 &&
+        payload_might_be_in_deck();
+  }
+
+  bool play_known_burnet_t3_steven_route() {
+    if (!known_burnet_t3_steven_route_available()) return false;
+
+    remove_one(state_.hand, Card::StevensResolve);
+    state_.discard.push_back(Card::StevensResolve);
+    state_.supporter_used = true;
+    outcome_.used_steven = true;
+    record_deck_search_knowledge("Steven's Resolve");
+
+    if (!move_deck_to_hand(Card::RegidragoVstar) ||
+        !move_deck_to_hand(Card::ProfessorBurnet)) {
+      throw std::logic_error("Known Burnet T3 Steven targets disappeared");
+    }
+    shuffle(state_.deck);
+    state_.turn_ended = true;
+
+    // Steven searches up to three unrestricted cards. Taking only VSTAR and Burnet
+    // preserves the remaining deck payload for Burnet's next-turn search:
+    // https://api.pokemontcg.io/v2/cards/sm7-145
+    // https://api.pokemontcg.io/v2/cards/swsh12tg-TG26
+    // https://github.com/FlareZ123/pokemon-sims/issues/1108
+    trace("PLAY SUPPORTER",
+          "R-STEVEN-01; R-GAME-SUPPORTER; P-KNOWLEDGE-01; P-COMPRESS-01",
+          "Searched the known T3 Burnet route: Regidrago VSTAR, Professor Burnet. Turn ended.");
+    return true;
+  }
+
   bool known_turo_t4_steven_route_available() const {
     if (!deck_seen_ || state_.turn != 3 || scenario_.going_first ||
         scenario_.dci != DciProfile::StrictJit ||
@@ -78,6 +136,7 @@
 
   bool should_play_steven() const {
     return should_play_steven_issue1030_original() ||
+        known_burnet_t3_steven_route_available() ||
         known_turo_t4_steven_route_available();
   }
 
@@ -90,6 +149,7 @@
     // Earliest complete route: https://github.com/FlareZ123/pokemon-sims/blob/main/docs/POLICY_DECISIONS.md#decision-priorities
     // Confirmed bug: https://github.com/FlareZ123/pokemon-sims/issues/1067
     if (held_arven_fss_blender_finish_available()) return false;
+    if (play_known_burnet_t3_steven_route()) return true;
     if (play_known_turo_t4_steven_route()) return true;
     return play_steven_issue1030_original();
   }
diff --git a/tests/issue_1108_known_burnet_steven_route_tests.cpp b/tests/issue_1108_known_burnet_steven_route_tests.cpp
new file mode 100644
index 0000000..52d36aa
--- /dev/null
+++ b/tests/issue_1108_known_burnet_steven_route_tests.cpp
@@ -0,0 +1,66 @@
+#define REGIDRAGO_SIM_NO_MAIN
+#include "../src/regidrago_sim.cpp"
+
+#include <algorithm>
+#include <random>
+#include <stdexcept>
+#include <string>
+
+namespace sim {
+struct EngineTestAccess {};
+}  // namespace sim
+
+namespace {
+void expect(const bool condition, const char* message) {
+  if (!condition) throw std::runtime_error(message);
+}
+
+bool contains(const sim::TraceLog& trace, const std::string& needle) {
+  return std::any_of(trace.lines.begin(), trace.lines.end(),
+                     [&needle](const std::string& line) {
+                       return line.find(needle) != std::string::npos;
+                     });
+}
+
+bool contains_line(const sim::TraceLog& trace, const std::string& first,
+                   const std::string& second) {
+  return std::any_of(trace.lines.begin(), trace.lines.end(),
+                     [&first, &second](const std::string& line) {
+                       return line.find(first) != std::string::npos &&
+                           line.find(second) != std::string::npos;
+                     });
+}
+
+void test_seed_101_reserves_vstar_and_burnet() {
+  const auto scenario = sim::scenario_by_label("strict-jit/go-second");
+  if (!scenario) throw std::runtime_error("Missing scenario");
+
+  sim::DeckRecipe recipe{sim::baseline_recipe()};
+  std::mt19937_64 rng{101};
+  sim::TraceLog trace;
+  trace.enabled = true;
+  sim::Engine engine(*scenario, recipe, rng, &trace);
+  const sim::TrialOutcome outcome = engine.run();
+
+  // Steven reserves the known Evolution and direct deck-to-discard Supporter on T2.
+  // Burnet then establishes the strict-JIT payload on the same T3 that V evolves:
+  // https://api.pokemontcg.io/v2/cards/sm7-145
+  // https://api.pokemontcg.io/v2/cards/swsh12tg-TG26
+  // https://api.pokemontcg.io/v2/cards/swsh12-136
+  // https://www.pokemon.com/us/pokemon-tcg/rules
+  // https://github.com/FlareZ123/pokemon-sims/issues/1108
+  expect(outcome.first_ready_turn == 3,
+         "Seed 101 must reach strict-JIT readiness on T3.");
+  expect(contains(trace, "known T3 Burnet route"),
+         "The T2 trace must reserve VSTAR and Professor Burnet.");
+  expect(contains_line(trace, "PLAY SUPPORTER", "Professor Burnet"),
+         "The T3 trace must play Professor Burnet.");
+  expect(!contains(trace, "exchanged Gladion for Regidrago V"),
+         "Gladion must not preempt the known T3 completion.");
+}
+}  // namespace
+
+int main() {
+  test_seed_101_reserves_vstar_and_burnet();
+  return 0;
+}
