name: CI

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  delete-cleanup-branches:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Delete merged cleanup branches
        run: git push origin --delete cleanup-1786945480000 cleanup-1786944390000
