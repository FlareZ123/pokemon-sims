name: CI

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  release:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Configure Release
        run: cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release
      - name: Build simulator
        run: cmake --build build --parallel 2 --target regidrago_sim
      - name: Probe issue-1552 equivalent go-second state
        shell: bash
        run: |
          set -o pipefail
          # Earthen Vessel: https://api.pokemontcg.io/v2/cards/sv4-163
          # Mysterious Treasure: https://api.pokemontcg.io/v2/cards/sm6-113
          # Quick Ball: https://api.pokemontcg.io/v2/cards/swsh1-179
          # Tapu Lele-GX: https://api.pokemontcg.io/v2/cards/sm2-60
          # Crispin: https://api.pokemontcg.io/v2/cards/sv7-133
          # Regidrago VSTAR: https://api.pokemontcg.io/v2/cards/swsh12-136
          # Advanced rules: https://github.com/FlareZ123/pokemon-sims/blob/main/EN_advanced_manual-2025-transcription-structured.md
          ./build/regidrago_sim --simulate-this --scenario strict-jit/go-second --seed 104 --require-ready-by 2 | tee trace-go-second-104.txt
