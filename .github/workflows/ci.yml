name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: ci-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  release:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Configure Release
        run: cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release
      - name: Build simulator and unified tests
        run: cmake --build build --parallel 2
      - name: Run eight independent simulate-this audits
        shell: bash
        run: |
          set -euo pipefail
          ./build/regidrago_sim --simulate-this --scenario strict-jit/go-second --seed 1 --require-ready-by 3 | tee trace-1.txt
          ./build/regidrago_sim --simulate-this --scenario strict-jit/go-first --seed 2 --require-ready-by 4 | tee trace-2.txt
          ./build/regidrago_sim --simulate-this --scenario matchup-flex-jit/go-second --seed 3 --require-ready-by 2 | tee trace-3.txt
          # Steven may search up to three cards. Held Fire plus target-legal Quick Ball
          # already completes the T3 Energy and strict-JIT payload axes, so preserve
          # Crispin and the one-copy ACE SPEC after taking the missing VSTAR card:
          # https://api.pokemontcg.io/v2/cards/sm7-145
          # https://api.pokemontcg.io/v2/cards/swsh1-179
          # https://api.pokemontcg.io/v2/cards/sv6-130
          # https://api.pokemontcg.io/v2/cards/swsh12-136
          # https://api.pokemontcg.io/v2/cards/sv8-164
          # https://www.pokemon.com/us/pokemon-tcg/rules
          # https://github.com/FlareZ123/pokemon-sims/issues/952
          # https://github.com/FlareZ123/pokemon-sims/issues/978
          ./build/regidrago_sim --simulate-this --scenario strict-jit/go-first --seed 58 --require-ready-by 3 | tee trace-4.txt
          grep -q "Searched up to 3 cards: Regidrago VSTAR. Turn ended." trace-4.txt
          grep -q "T3 | DISCARD.*Dragapult ex (Quick Ball cost)" trace-4.txt
          ! grep -q "Searched up to 3 cards: Regidrago VSTAR, Crispin" trace-4.txt
          ! grep -q "Searched up to 3 cards: Regidrago VSTAR, Brilliant Blender" trace-4.txt
          grep -q "T3 | READY" trace-4.txt
          # Quick Ball must preserve the strict-JIT Dragon until the VSTAR card axis is reachable:
          # https://api.pokemontcg.io/v2/cards/swsh1-179
          # https://api.pokemontcg.io/v2/cards/swsh12-136
          # https://www.pokemon.com/us/pokemon-tcg/rules
          # https://github.com/FlareZ123/pokemon-sims/issues/953
          ./build/regidrago_sim --simulate-this --scenario strict-jit/go-second --seed 19 --require-ready-by 3 | tee trace-5.txt
          # Earthen Vessel must preserve its one-discard payload outlet until VSTAR is reachable:
          # https://api.pokemontcg.io/v2/cards/sv4-163
          # https://api.pokemontcg.io/v2/cards/swsh12-136
          # https://www.pokemon.com/us/pokemon-tcg/rules
          # https://github.com/FlareZ123/pokemon-sims/issues/954
          ./build/regidrago_sim --simulate-this --scenario strict-jit/go-first --seed 42 --require-ready-by 3 | tee trace-6.txt
          # Quick Ball must take the live Tapu Lele-GX -> Crispin connector before Vessel:
          # https://api.pokemontcg.io/v2/cards/swsh1-179
          # https://api.pokemontcg.io/v2/cards/cel25c-60_A
          # https://api.pokemontcg.io/v2/cards/sv7-133
          # https://api.pokemontcg.io/v2/cards/sv4-163
          # https://www.pokemon.com/us/pokemon-tcg/rules
          # https://github.com/FlareZ123/pokemon-sims/issues/962
          ./build/regidrago_sim --simulate-this --scenario strict-jit/go-first --seed 104 --require-ready-by 3 | tee trace-7.txt
          grep -q "Searched a Basic Pokémon: Tapu Lele-GX" trace-7.txt
          grep -q "WONDER TAG.*Searched and revealed Crispin" trace-7.txt
          # A duplicate Regidrago VSTAR may pay Quick Ball only while another copy
          # remains protected for the Regidrago V established by the search:
          # https://api.pokemontcg.io/v2/cards/swsh1-179
          # https://api.pokemontcg.io/v2/cards/swsh12-135
          # https://api.pokemontcg.io/v2/cards/swsh12-136
          # https://www.pokemon.com/us/pokemon-tcg/rules
          # https://github.com/FlareZ123/pokemon-sims/issues/964
          ./build/regidrago_sim --simulate-this --scenario strict-jit/go-first --seed 43 --require-ready-by 3 | tee trace-8.txt
          grep -q "DISCARD.*Regidrago VSTAR (Quick Ball cost)" trace-8.txt
          grep -q "Searched a Basic Pokémon: Regidrago V" trace-8.txt
          # Once held Mysterious Treasure plus Dragapult ex supplies strict-JIT payload,
          # Star Alchemy takes the direct Energy Supporter instead of unused Blender:
          # https://api.pokemontcg.io/v2/cards/swsh12-156
          # https://api.pokemontcg.io/v2/cards/sv7-133
          # https://api.pokemontcg.io/v2/cards/sm6-113
          # https://api.pokemontcg.io/v2/cards/sv6-130
          # https://github.com/FlareZ123/pokemon-sims/issues/976
          grep -q "STAR ALCHEMY.*Crispin" trace-8.txt
          grep -q "DISCARD.*Dragapult ex.*Mysterious Treasure cost" trace-8.txt
          ! grep -q "STAR ALCHEMY.*Brilliant Blender" trace-8.txt
          grep -q "T3 | READY" trace-8.txt
      # The issue #718 PR refreshes and validates the source-bound matrix after the
      # Blender VSTAR-card-axis fix: https://github.com/FlareZ123/pokemon-sims/pull/931
      - name: Generate canonical T2 and T3 matrix
        run: ./build/regidrago_sim --trials 100000 --seed 20260705 --out t2-t3-matrix.csv
      - name: Record simulator policy source digest
        run: >-
          python -c "from pathlib import Path; from scripts.baseline_provenance import simulator_policy_source_digest; print(simulator_policy_source_digest(Path('.').resolve()))"
          | tee simulator-policy-source-sha256.txt
      - name: Run complete Release suite
        run: ctest --test-dir build --parallel 4 --output-on-failure
      - name: Record tested source
        run: git rev-parse HEAD > tested-head.txt
      - name: Upload validation evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: release-validation-${{ github.run_attempt }}
          path: |
            trace-*.txt
            t2-t3-matrix.csv
            simulator-policy-source-sha256.txt
            tested-head.txt
            build/regidrago_sim
            build/regidrago_unified_tests
            build/Testing
          if-no-files-found: warn

  sanitizers:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Configure ASan and UBSan
        run: >-
          cmake -S . -B build-sanitizers -G Ninja -DCMAKE_BUILD_TYPE=Debug
          -DCMAKE_CXX_FLAGS="-fsanitize=address,undefined -fno-omit-frame-pointer"
          -DCMAKE_EXE_LINKER_FLAGS="-fsanitize=address,undefined"
      - name: Build sanitizer targets once
        run: cmake --build build-sanitizers --parallel 2
      - name: Run complete sanitizer suite
        env:
          # Detect the reference-parameter lifetime failure covered by issue #907:
          # https://eel.is/c++draft/class.temporary#6.10
          # https://github.com/FlareZ123/pokemon-sims/issues/907
          ASAN_OPTIONS: detect_stack_use_after_return=1:detect_leaks=1:halt_on_error=1
          UBSAN_OPTIONS: halt_on_error=1
        run: ctest --test-dir build-sanitizers --parallel 2 --output-on-failure
      - name: Upload sanitizer diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: sanitizer-validation-${{ github.run_attempt }}
          path: build-sanitizers/Testing
          if-no-files-found: warn

  materialize-issue-1185:
    if: github.event_name == 'pull_request' && github.head_ref == 'fix/1185-strict-jit-surplus-energy'
    runs-on: ubuntu-24.04
    timeout-minutes: 75
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Extract and execute the source-bound materializer
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path
          import subprocess
          import textwrap

          workflow = Path('.github/workflows/issue-1185-materialize-and-validate.yml').read_text(encoding='utf-8')
          start_marker = '      - name: Materialize the confirmed route fix and focused regressions\n        shell: bash\n        run: |\n'
          end_marker = '      - name: Configure Release\n'
          start = workflow.index(start_marker) + len(start_marker)
          end = workflow.index(end_marker, start)
          subprocess.run(['bash', '-lc', textwrap.dedent(workflow[start:end])], check=True)
          PY
      - name: Configure and build Release
        run: |
          cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release
          cmake --build build --parallel 2
      - name: Run required independent traces
        shell: bash
        run: |
          set -euo pipefail
          ./build/regidrago_sim --simulate-this --scenario strict-jit/go-first --seed 23 | tee issue-1185-seed-23.txt # https://github.com/FlareZ123/pokemon-sims/issues/1185
          grep -q "T5 | READY" issue-1185-seed-23.txt
          grep -q "T5 | PLAY SUPPORTER.*R-BURNET-01" issue-1185-seed-23.txt # https://api.pokemontcg.io/v2/cards/swsh12tg-TG26
          ./build/regidrago_sim --simulate-this --scenario strict-jit/go-second --seed 2 --require-ready-by 3 | tee pass-2.txt # https://github.com/FlareZ123/pokemon-sims/blob/main/README.md#run-one-readable-hand
          ./build/regidrago_sim --simulate-this --scenario matchup-flex-jit/go-second --seed 73 --require-ready-by 2 | tee pass-3.txt # https://github.com/FlareZ123/pokemon-sims/blob/main/docs/POLICY_DECISIONS.md#dcijit-treatment
      - name: Regenerate source-bound setup baselines
        run: |
          rm -f .results.lock
          python scripts/regenerate_setup_baselines.py --exe build/regidrago_sim --out-dir results
          python scripts/update_setup_docs.py
      - name: Run complete Release suite
        run: ctest --test-dir build --parallel 4 --output-on-failure
      - name: Commit validated implementation and evidence
        shell: bash
        run: |
          rm -f .issue-1185-materializer.lock
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/trace_engine_v2/part_009b1.inc tests/issue_1146_quick_ball_surplus_energy_tests.cpp tests/issue_1185_quick_ball_strict_surplus_energy_tests.cpp results docs
          if ! git diff --cached --quiet; then
            git commit -m "Fix strict-JIT surplus Energy recovery (#1185)"
            git push origin HEAD:${{ github.head_ref }}
          fi
      - name: Upload exact matrix and traces
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: issue-1185-materialized-${{ github.run_attempt }}
          path: |
            issue-1185-seed-23.txt
            pass-2.txt
            pass-3.txt
            results/simulation_results.csv
            results/baseline_manifest.json
            build/Testing
          if-no-files-found: warn
