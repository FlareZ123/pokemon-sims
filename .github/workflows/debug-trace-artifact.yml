name: Debug delete cleanup branch

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  delete:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Delete merged cleanup branch
        run: git push origin --delete cleanup-1786819027105
