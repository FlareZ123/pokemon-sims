name: Trace Audit Generator

on:
  pull_request:

jobs:
  generator-regression:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Validate independent trace claims
        run: python tests/update_setup_docs_tests.py
