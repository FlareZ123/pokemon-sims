name: Ultra Ball Cross-Singleton Route

on:
  pull_request:

jobs:
  focused-regression:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        mode: [release, sanitizers]
    steps:
      - uses: actions/checkout@v4
      - name: Build focused regression
        shell: bash
        run: |
          flags="-std=c++20 -Wall -Wextra -Wpedantic -Wconversion -Wshadow -Werror -UNDEBUG"
          if [[ "${{ matrix.mode }}" == "sanitizers" ]]; then
            flags="$flags -fsanitize=address,undefined -fno-omit-frame-pointer"
          else
            flags="$flags -O2"
          fi
          g++ $flags tests/ultra_ball_cross_singleton_cost_tests.cpp -o ultra_ball_cross_singleton_tests
      - name: Run focused regression
        shell: bash
        run: |
          if [[ "${{ matrix.mode }}" == "sanitizers" ]]; then
            ASAN_OPTIONS=detect_leaks=1:halt_on_error=1 ./ultra_ball_cross_singleton_tests
          else
            ./ultra_ball_cross_singleton_tests
          fi
