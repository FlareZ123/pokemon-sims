# Cleanup CI audit

Behavior-neutral marker used to trigger pull-request CI after the 2026-08-10 composition cleanup commits on `main`. It changes no simulator, policy, deck, test, or generated-evidence inputs.
