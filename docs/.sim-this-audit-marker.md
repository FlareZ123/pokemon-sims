# Temporary CI simulate-this audit marker

Audit pass: 2

This branch is diagnostic only. It exists to trigger an unchanged pull-request CI run so the repository's emitted `--simulate-this` traces can be reviewed independently. It will not be merged.
