# CI audit 2026-08-09

Behavior-neutral marker used to trigger pull-request CI from main commit `16d5f0fd71c7804789749de4cc8c34035d9d9aa2` after the required cleanup commits. This branch does not alter simulator policy, card data, deck recipes, or production game logic.

The audit requires the repository's permanent simulator trace checks, full Release and sanitizer validation, and source-bound setup matrices. The branch is intended to be closed without merge after evidence review.
