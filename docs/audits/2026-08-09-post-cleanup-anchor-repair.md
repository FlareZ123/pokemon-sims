# Post-cleanup anchor repair validation

Documentation-only CI trigger rooted at `596643252ffca787bc2a303d538cfe11b2726773`.

The legacy trace-engine source files remain canonical and line-addressable. The new `core/` and `cli/` paths are structured aliases only.

Synchronization trigger: second pull-request CI event after the initial Actions event did not register.
