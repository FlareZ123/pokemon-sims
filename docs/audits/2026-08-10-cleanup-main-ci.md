# Cleanup main CI audit

Behavior-neutral pull-request trigger created after the direct-main `Cleanup:` composition refactors on 2026-08-10. CI must compile the collapsed composition spine, run the release suite, execute the registered `--simulate-this` trace audits, and regenerate the T2/T3 matrix without semantic drift.
