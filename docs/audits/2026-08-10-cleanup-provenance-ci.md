# Post-cleanup provenance CI audit

Behavior-neutral pull-request trigger after the direct-main cleanup composition refactors and regenerated provenance metadata. CI must confirm strict C++20 compilation, all registered `--simulate-this` audits, byte-identical committed matrices, source provenance contracts, and the complete release/sanitizer suites.
