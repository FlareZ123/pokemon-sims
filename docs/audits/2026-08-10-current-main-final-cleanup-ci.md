# Current-main cleanup verification

Behavior-neutral pull-request trigger after the direct-main cleanup refactors, concurrent composition folds, and provenance refresh. CI must validate strict compilation, all registered `--simulate-this` traces, canonical and paired 100,000-trial matrices, generated-report provenance, and the release/sanitizer suites.
