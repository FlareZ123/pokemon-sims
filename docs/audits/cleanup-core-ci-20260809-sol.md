# Cleanup core CI audit

This PR-only marker exists to trigger the repository pull-request CI against main commit `edf043ed022dfb0e0492acf1c79919528b8a4b1d` after the three direct-main `Cleanup:` commits from the 2026-08-09 audit.

The marker carries no simulator or policy changes and is not intended for merge.
