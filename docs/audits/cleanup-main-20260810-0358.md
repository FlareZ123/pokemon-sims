# Cleanup main CI audit

Validation-only marker for the direct-main cleanup composition series ending with `Cleanup: compose Arven overrides by family`.

This branch contains no simulator source changes beyond its `main` base. The marker exists only to trigger pull-request CI against that exact cleanup head.
