# Post-cleanup CI audit

Behavior-neutral marker used only to trigger pull-request CI against the post-cleanup `main` tree on 2026-08-09.

The audit validates the permanent `--simulate-this` trace suite, canonical and paired 100,000-trial setup matrices, complete Release tests, and sanitizers. This marker is not intended to merge.
