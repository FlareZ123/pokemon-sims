# Cleanup main CI audit

Behavior-neutral PR used only to run pull-request CI against `main@ca72161f34395c748b9e8da12ec632fd32c5a2b6` after the three required cleanup commits.

The only branch-local change is this audit note. The simulator code under test is the cleanup state already committed directly to `main`.
