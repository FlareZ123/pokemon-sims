# Cleanup CI audit

This branch exists only to exercise pull-request CI against the direct `main` cleanup commits from 2026-08-10. The cleanup changes extract existing include/macro composition blocks without changing simulator semantics.
