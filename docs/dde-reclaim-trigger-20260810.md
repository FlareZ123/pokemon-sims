DDE semantic rollup reclaim synchronization trigger. Reclaimed at 2026-08-10T18:21:15-04:00 after the shared PR remained stale for more than four hours, non-mergeable, and without a head workflow run. This temporary file is removed by the main-branch recovery workflow after it merges current main, resolves only the audited overlap set, validates the focused DDE suite, and runs three fixed-seed --simulate-this traces.