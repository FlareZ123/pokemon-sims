# Final validation marker

This documentation-only marker exists to run pull-request CI against the exact simulator source currently on `main` after the required cleanup commits. It is not intended to merge.
