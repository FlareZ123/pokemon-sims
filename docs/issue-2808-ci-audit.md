# Issue 2808 CI audit

This branch exists only to force a pull-request CI run against the current `fix/2808-garbodor-shake-ability-lock` source head after GitHub Actions did not register a synchronize run. The only branch-specific change is this audit note.
