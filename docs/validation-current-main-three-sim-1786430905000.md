# Exact current-main debug audit

Behavior-neutral marker for CI validation of `main@a96fcf48bfeadd224a01827be6d5c0ee3092416d`.

This branch exists only to run the repository CI, inspect the permanent `--simulate-this` traces, and obtain a source-bound T2/T3 setup matrix. It does not change simulator policy.
