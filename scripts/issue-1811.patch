diff --git a/src/trace_engine_v2/part_012_override.inc b/src/trace_engine_v2/part_012_override.inc
index 9017c01986304bb356ff15600292c170d3d38c4c..31edc729fd802b1f279bf19bbde5d323b20a8ad1 100644
--- a/src/trace_engine_v2/part_012_override.inc
+++ b/src/trace_engine_v2/part_012_override.inc
@@ -233,7 +233,20 @@
       if (need_vstar() && live_vstar_target) {
         add_item_candidate(Card::EvolutionIncense, true);
         add_item_candidate(Card::PokemonCommunication, live_communication);
-        add_item_candidate(Card::MysteriousTreasure, can_pay_search_cost);
+        // Arven itself establishes K1. Re-evaluate the exact post-inspection route
+        // when the generic pre-Arven discard gate protects a Fire Energy that T2
+        // Crispin deterministically replaces:
+        // Arven: https://api.pokemontcg.io/v2/cards/sv1-166
+        // Mysterious Treasure: https://api.pokemontcg.io/v2/cards/sm6-113
+        // Forest Seal Stone: https://api.pokemontcg.io/v2/cards/swsh12-156
+        // Crispin: https://api.pokemontcg.io/v2/cards/sv7-133
+        // Earthen Vessel: https://api.pokemontcg.io/v2/cards/sv4-163
+        // Regidrago VSTAR: https://api.pokemontcg.io/v2/cards/swsh12-136
+        // K1, dynamic DCI, strict-JIT, and route priority: https://github.com/FlareZ123/pokemon-sims/blob/main/docs/POLICY_DECISIONS.md#knowledge-states https://github.com/FlareZ123/pokemon-sims/blob/main/docs/MODEL_ASSUMPTIONS.md#dci-implementation https://github.com/FlareZ123/pokemon-sims/blob/main/docs/POLICY_DECISIONS.md#dcijit-treatment https://github.com/FlareZ123/pokemon-sims/blob/main/docs/POLICY_DECISIONS.md#decision-priorities
+        // Confirmed bug: https://github.com/FlareZ123/pokemon-sims/issues/1811
+        add_item_candidate(
+            Card::MysteriousTreasure,
+            can_pay_search_cost || issue_1811_arven_treasure_crispin_route_available());
       }
       if (need_energy() && live_energy_target) {
         add_item_candidate(Card::PokemonCommunication, live_communication);
diff --git a/src/trace_engine_v2/part_014c_latias_bench_override.inc b/src/trace_engine_v2/part_014c_latias_bench_override.inc
index 735bc7f93f93db480cc170f8a13017934996d0b2..a347c065b381d7c9e5693188824f65fdfa883c60 100644
--- a/src/trace_engine_v2/part_014c_latias_bench_override.inc
+++ b/src/trace_engine_v2/part_014c_latias_bench_override.inc
@@ -623,6 +623,21 @@ void run_turn() {
       return;
     }
 
+    // Arven resolves before the searched Item and Tool can be played. When its K1
+    // inspection proves the complete issue-1811 route, play Mysterious Treasure
+    // before attaching Forest Seal Stone so VSTAR and Crispin occupy separate search
+    // channels in the legal order:
+    // Arven: https://api.pokemontcg.io/v2/cards/sv1-166
+    // Mysterious Treasure: https://api.pokemontcg.io/v2/cards/sm6-113
+    // Forest Seal Stone: https://api.pokemontcg.io/v2/cards/swsh12-156
+    // Crispin: https://api.pokemontcg.io/v2/cards/sv7-133
+    // Official Supporter, Item, Tool, and VSTAR Power procedure: https://www.pokemon.com/static-assets/content-assets/cms2/pdf/trading-card-game/rulebook/par_rulebook_en.pdf
+    // Earliest-route specification: https://github.com/FlareZ123/pokemon-sims/blob/main/docs/POLICY_DECISIONS.md#decision-priorities
+    // Confirmed bug: https://github.com/FlareZ123/pokemon-sims/issues/1811
+    if (issue_1811_arven_treasure_crispin_route_available()) {
+      play_mysterious_treasure(false);
+    }
+
     play_chaotic_swell();
     play_field_blower();
     play_basics_from_hand();
diff --git a/src/trace_engine_v2/part_issue_1356_fss_energy_override.inc b/src/trace_engine_v2/part_issue_1356_fss_energy_override.inc
index 0c088d1157592f97f0d28e9843d6e59628d7a9bd..5ddeeab0fd90e0aadbe460487d8526b96d8f1ec3 100644
--- a/src/trace_engine_v2/part_issue_1356_fss_energy_override.inc
+++ b/src/trace_engine_v2/part_issue_1356_fss_energy_override.inc
@@ -79,6 +79,36 @@
     return star_takes_fire || star_takes_grass;
   }
 
+  bool issue_1811_fss_crispin_continuation_available() const {
+    if (scenario_.dci != DciProfile::StrictJit || scenario_.going_first ||
+        scenario_.locks != LockMode::None || state_.turn != 1 ||
+        scenario_.max_turn < 2 || !deck_seen_ || item_locked() ||
+        !state_.supporter_used || !state_.manual_energy_used ||
+        state_.vstar_power_used || !state_.active ||
+        state_.active->card != Card::RegidragoV ||
+        state_.active->grass != 1 || state_.active->fire != 0 ||
+        state_.active->tool != Tool::ForestSealStone ||
+        hand_count(Card::RegidragoVstar) == 0 ||
+        hand_count(Card::EarthenVessel) == 0 ||
+        !std::any_of(state_.hand.begin(), state_.hand.end(), is_payload)) {
+      return false;
+    }
+
+    // Mysterious Treasure has already supplied Regidrago VSTAR. Star Alchemy now
+    // takes Crispin, whose two-type search supplies both T2 Energy attachments. The
+    // held Vessel and Dragon preserve the same-turn strict-JIT payload channel:
+    // Forest Seal Stone: https://api.pokemontcg.io/v2/cards/swsh12-156
+    // Crispin: https://api.pokemontcg.io/v2/cards/sv7-133
+    // Earthen Vessel: https://api.pokemontcg.io/v2/cards/sv4-163
+    // Regidrago VSTAR: https://api.pokemontcg.io/v2/cards/swsh12-136
+    // Official VSTAR Power, Supporter, attachment, evolution, and Item procedure: https://www.pokemon.com/static-assets/content-assets/cms2/pdf/trading-card-game/rulebook/par_rulebook_en.pdf
+    // Strict-JIT and route-priority specifications: https://github.com/FlareZ123/pokemon-sims/blob/main/docs/POLICY_DECISIONS.md#dcijit-treatment https://github.com/FlareZ123/pokemon-sims/blob/main/docs/POLICY_DECISIONS.md#decision-priorities
+    // Confirmed bug: https://github.com/FlareZ123/pokemon-sims/issues/1811
+    return deck_count_after_search_started(Card::Crispin) > 0 &&
+        deck_count_after_search_started(Card::Grass) > 0 &&
+        deck_count_after_search_started(Card::Fire) > 0;
+  }
+
   bool use_fss() {
     const auto live_fss_holder = [](const Pokemon& pokemon) {
       return pokemon.tool == Tool::ForestSealStone && is_pokemon_v(pokemon.card);
@@ -86,6 +116,26 @@
     const bool forest_seal_stone_is_live = !state_.vstar_power_used &&
         ((state_.active && live_fss_holder(*state_.active)) ||
          std::any_of(state_.bench.begin(), state_.bench.end(), live_fss_holder));
+    if (forest_seal_stone_is_live &&
+        issue_1811_fss_crispin_continuation_available()) {
+      state_.vstar_power_used = true;
+      outcome_.used_fss = true;
+      record_deck_search_knowledge("Star Alchemy");
+      if (!move_deck_to_hand(Card::Crispin)) {
+        throw std::logic_error("Issue-1811 Crispin target disappeared");
+      }
+      shuffle(state_.deck);
+      // Star Alchemy searches Crispin only after Mysterious Treasure has supplied
+      // VSTAR, preserving one search channel for each missing axis:
+      // Forest Seal Stone: https://api.pokemontcg.io/v2/cards/swsh12-156
+      // Crispin: https://api.pokemontcg.io/v2/cards/sv7-133
+      // Regidrago VSTAR: https://api.pokemontcg.io/v2/cards/swsh12-136
+      // Confirmed bug: https://github.com/FlareZ123/pokemon-sims/issues/1811
+      trace("STAR ALCHEMY", "R-FSS-02; R-VSTAR-01; P-COMPRESS-01",
+            "Searched Crispin for the deterministic T2 Energy package after Mysterious Treasure supplied Regidrago VSTAR.");
+      return true;
+    }
+
     if (forest_seal_stone_is_live &&
         issue_1393_held_crispin_completion_available()) {
       // A held Crispin can attach one searched Basic Energy and place the other
diff --git a/src/trace_engine_v2/part_issue_1356_fss_treasure_energy_override.inc b/src/trace_engine_v2/part_issue_1356_fss_treasure_energy_override.inc
index cb4a645ae3d3537a9fcd86bd9db1cd0c1b8bd500..da2616e998bf79fea1eba4828b70f4ca3be4d57c 100644
--- a/src/trace_engine_v2/part_issue_1356_fss_treasure_energy_override.inc
+++ b/src/trace_engine_v2/part_issue_1356_fss_treasure_energy_override.inc
@@ -99,9 +99,81 @@
     return play_quick_ball_issue1356_original(permit_payload);
   }
 
+  bool issue_1811_arven_treasure_crispin_route_available() const {
+    if (scenario_.dci != DciProfile::StrictJit || scenario_.going_first ||
+        scenario_.locks != LockMode::None || state_.turn != 1 ||
+        scenario_.max_turn < 2 || !deck_seen_ || item_locked() ||
+        !state_.supporter_used || count_of(state_.discard, Card::Arven) == 0 ||
+        !state_.manual_energy_used || state_.vstar_power_used || !state_.active ||
+        state_.active->card != Card::RegidragoV ||
+        state_.active->grass != 1 || state_.active->fire != 0 ||
+        state_.active->tool != Tool::None || !need_vstar() ||
+        !ability_available_for_pokemon(Card::RegidragoV) ||
+        hand_count(Card::Fire) == 0 || hand_count(Card::EarthenVessel) == 0 ||
+        !std::any_of(state_.hand.begin(), state_.hand.end(), is_payload)) {
+      return false;
+    }
+
+    const bool treasure_available =
+        hand_count(Card::MysteriousTreasure) > 0 ||
+        deck_count_after_search_started(Card::MysteriousTreasure) > 0;
+    const bool fss_available = hand_count(Card::ForestSealStone) > 0 ||
+        deck_count_after_search_started(Card::ForestSealStone) > 0;
+
+    // Arven has legally exposed the deck. Mysterious Treasure can spend the held
+    // Fire and search Regidrago VSTAR because Star Alchemy can bank Crispin. On T2,
+    // Crispin searches Grass and Fire, attaches one, and the fresh manual attachment
+    // supplies the other. Held Earthen Vessel then discards a held Dragon during the
+    // ready turn, satisfying strict JIT without any future-card knowledge:
+    // Arven: https://api.pokemontcg.io/v2/cards/sv1-166
+    // Mysterious Treasure: https://api.pokemontcg.io/v2/cards/sm6-113
+    // Forest Seal Stone: https://api.pokemontcg.io/v2/cards/swsh12-156
+    // Crispin: https://api.pokemontcg.io/v2/cards/sv7-133
+    // Earthen Vessel: https://api.pokemontcg.io/v2/cards/sv4-163
+    // Regidrago V / VSTAR: https://api.pokemontcg.io/v2/cards/swsh12-135 https://api.pokemontcg.io/v2/cards/swsh12-136
+    // Official Supporter, Item, Tool, VSTAR Power, attachment, evolution, and search procedure: https://www.pokemon.com/static-assets/content-assets/cms2/pdf/trading-card-game/rulebook/par_rulebook_en.pdf
+    // K1, dynamic DCI, strict-JIT, and earliest-route specifications: https://github.com/FlareZ123/pokemon-sims/blob/main/docs/POLICY_DECISIONS.md#knowledge-states https://github.com/FlareZ123/pokemon-sims/blob/main/docs/MODEL_ASSUMPTIONS.md#dci-implementation https://github.com/FlareZ123/pokemon-sims/blob/main/docs/POLICY_DECISIONS.md#dcijit-treatment https://github.com/FlareZ123/pokemon-sims/blob/main/docs/POLICY_DECISIONS.md#decision-priorities
+    // Confirmed bug: https://github.com/FlareZ123/pokemon-sims/issues/1811
+    return treasure_available && fss_available &&
+        deck_count_after_search_started(Card::RegidragoVstar) > 0 &&
+        deck_count_after_search_started(Card::Crispin) > 0 &&
+        deck_count_after_search_started(Card::Grass) > 0 &&
+        deck_count_after_search_started(Card::Fire) > 0;
+  }
+
   bool play_mysterious_treasure(const bool permit_payload) {
     if (issue_1817_play_prelock_treasure_tapu_route()) return true;
     if (issue_1411_play_treasure_tapu_steven_route()) return true;
+    const auto generic_cost = choose_discard(
+        permit_payload, true, true, Card::MysteriousTreasure);
+    if (issue_1811_arven_treasure_crispin_route_available() && !generic_cost) {
+      remove_one(state_.hand, Card::MysteriousTreasure);
+      state_.discard.push_back(Card::MysteriousTreasure);
+      if (!discard_from_hand(Card::Fire,
+                             "Mysterious Treasure Arven-Crispin route cost",
+                             "R-MT-01; P-DCI-01; P-COMPRESS-01")) {
+        throw std::logic_error(
+            "projected issue-1811 route-replaced Fire cost disappeared");
+      }
+      record_deck_search_knowledge("Mysterious Treasure");
+      if (!move_deck_to_hand(Card::RegidragoVstar)) {
+        throw std::logic_error(
+            "projected issue-1811 Regidrago VSTAR target disappeared");
+      }
+      shuffle(state_.deck);
+      // The held Fire is replaced by Crispin's two-type T2 search, while the
+      // searched VSTAR and held Vessel preserve the evolution and JIT payload axes:
+      // Mysterious Treasure: https://api.pokemontcg.io/v2/cards/sm6-113
+      // Crispin: https://api.pokemontcg.io/v2/cards/sv7-133
+      // Earthen Vessel: https://api.pokemontcg.io/v2/cards/sv4-163
+      // Regidrago VSTAR: https://api.pokemontcg.io/v2/cards/swsh12-136
+      // Dynamic DCI and earliest-route specifications: https://github.com/FlareZ123/pokemon-sims/blob/main/docs/MODEL_ASSUMPTIONS.md#dci-implementation https://github.com/FlareZ123/pokemon-sims/blob/main/docs/POLICY_DECISIONS.md#decision-priorities
+      // Confirmed bug: https://github.com/FlareZ123/pokemon-sims/issues/1811
+      trace("PLAY ITEM",
+            "R-MT-01; R-GAME-ITEM; P-DCI-01; P-COMPRESS-01",
+            "Discarded route-replaced Fire Energy and searched Regidrago VSTAR for the deterministic T2 Crispin route.");
+      return true;
+    }
     if (issue_1417_treasure_hold_turn_ == state_.turn) {
       // The recovered Mysterious Treasure is a next-turn connector. Playing it on
