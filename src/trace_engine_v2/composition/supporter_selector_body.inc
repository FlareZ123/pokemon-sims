#pragma once

// Historical compatibility boundary for the opening composition spine. The
// supporter selector alias implementation now has one owner in the adjacent
// composition unit while this include path remains stable.
// C++ textual include semantics: https://eel.is/c++draft/cpp.include
#include "supporter_selector_aliases.inc"
