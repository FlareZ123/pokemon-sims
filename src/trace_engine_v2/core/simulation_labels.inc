#pragma once

// Compatibility forwarder. Setup-facing deck/scenario labels now live beside the
// setup trace that consumes them, so the Engine has one setup-support owner.
// Canonical owner: https://github.com/FlareZ123/pokemon-sims/blob/main/src/trace_engine_v2/core/setup_lifecycle.inc
#include "setup_lifecycle.inc"
