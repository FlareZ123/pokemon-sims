// Keep the policy fixture's textual composition in one ordered spine.
// C++ textual include semantics: https://eel.is/c++draft/cpp.include

// Stable fixture state first, then ordered route regressions and registration.
#include "composition_baseline.inc"
#include "composition_routes.inc"
